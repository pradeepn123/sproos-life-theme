
[
    {% for block in section.blocks %}
      {
        "handle": "{{ block.settings.country_iso}}",
        "text": "{{ block.settings.announcement_text}}"
      }{% unless forloop.last %},{% endunless %}
    {% endfor %}
    ]